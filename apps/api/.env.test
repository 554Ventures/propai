# Test-only environment (used by pnpm -C apps/api test)
# Points to a dedicated test database so we can freely reset it.
DATABASE_URL=postgresql://propai:propai@localhost:5432/propai_test

# Avoid external moderation dependency in tests
AI_OUTPUT_MODERATION_ENABLED=false
